services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SHOWCASE_FAST_MODEL: ${SHOWCASE_FAST_MODEL:-openai:gpt-5-mini}
      SHOWCASE_RESEARCH_MODEL: ${SHOWCASE_RESEARCH_MODEL:-openai:gpt-5.2}
