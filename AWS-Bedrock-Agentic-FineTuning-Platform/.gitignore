# Secrets
.env
*.pem
credentials

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
# .terraform.lock.hcl is intentionally NOT ignored — it is committed

# Cloud credentials
.aws/
.azure/

# Frontend
node_modules/
dist/

# OS
.DS_Store

# Artifacts / build output
artifacts/

# Claude Code local session data. CLAUDE.md itself is intentionally NOT ignored — it is
# committed, as in the sibling projects, because its Project Invariants section documents
# the guardrails this build ran under. Also ignored at the monorepo root; repeated here so
# the folder stays self-contained if extracted on its own.
.claude/
