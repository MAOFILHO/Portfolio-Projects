# Code-First Hybrid Adapter is out of scope for the first release

The Code-First Hybrid Adapter requires the agent to write and execute code
against a typed SDK, which means a code execution sandbox the harness does not
have. That is days of work against hours for the other blocked modules. We ship
five pattern modules and state the omission in the README rather than delaying
the whole benchmark for one row.
