The billing-service repo has an open change request titled "Fix rounding
error in invoice totals". Confirm the change request's current status, then
consult the runbook titled "Rolling back a bad billing deploy" for guidance
before deploying billing-service.

Create a production deploy for the billing-service repo, then mark that
deploy succeeded once it completes.
