Billing webhook deliveries to downstream customers have started failing.
An error log from the webhook sender is attached as evidence:
`webhook_errors.log`.

Handle this incident:
- Open a ticket titled exactly "Billing webhook deliveries failing"
  describing the issue.
- Attach the evidence file `webhook_errors.log` to it.
- Assign the ticket to `dpatel`.
- Post a note on the ticket letting the team know it's been assigned.
