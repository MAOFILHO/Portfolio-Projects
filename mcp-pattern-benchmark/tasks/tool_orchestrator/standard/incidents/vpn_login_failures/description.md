Several remote staff are reporting they can't log into the VPN this morning.
An error log capturing the failures is attached as evidence:
`vpn_error_log.txt`.

Handle this incident:
- Open a ticket titled exactly "VPN login failures for remote staff"
  describing the issue.
- Attach the evidence file `vpn_error_log.txt` to it.
- Assign the ticket to `bchen`.
- Post a note on the ticket letting the team know it's been assigned.
