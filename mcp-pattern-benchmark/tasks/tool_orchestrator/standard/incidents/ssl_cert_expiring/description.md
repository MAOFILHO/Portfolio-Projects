The SSL certificate for api.internal expires in 48 hours and needs urgent
renewal. Details of the certificate are attached as evidence:
`cert_details.txt`.

Handle this incident:
- Open a ticket titled exactly "SSL certificate expiring on api.internal"
  describing the issue.
- Attach the evidence file `cert_details.txt` to it.
- Assign the ticket to `gnguyen`.
- Post a note on the ticket letting the team know it's been assigned.
