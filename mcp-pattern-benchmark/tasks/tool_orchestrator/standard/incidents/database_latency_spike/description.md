The orders service database is showing a sharp latency spike over the last
hour. A graph of the query latency is attached as evidence:
`latency_graph.png`.

Handle this incident:
- Open a ticket titled exactly "Database latency spike on orders service"
  describing the issue.
- Attach the evidence file `latency_graph.png` to it.
- Assign the ticket to `cwang`.
- Post a note on the ticket letting the team know it's been assigned.
