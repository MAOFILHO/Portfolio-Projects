# Report three metrics per pattern module, never a blended score

Success rate, turn count and input tokens are reported side by side for every
pattern module, with no weighting and no composite. A pattern module rarely
changes whether a task is possible; it changes what the task costs, and each
module is designed to move a different one of the three. A single number would
average away the mechanism, which is the only thing the result is evidence for.
