The build server is critically low on disk space and CI jobs have started
failing. A disk usage report is attached as evidence: `disk_usage.png`.

Handle this incident:
- Open a ticket titled exactly "Disk space critical on build server"
  describing the issue.
- Attach the evidence file `disk_usage.png` to it.
- Assign the ticket to `hoconnor`.
- Post a note on the ticket letting the team know it's been assigned.
