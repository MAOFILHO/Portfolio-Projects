The staging environment is broken after the last release -- the app fails
to start. The deploy output is attached as evidence: `deploy_output.txt`.

Handle this incident:
- Open a ticket titled exactly "Staging deploy broken after last release"
  describing the issue.
- Attach the evidence file `deploy_output.txt` to it.
- Assign the ticket to `egarcia`.
- Post a note on the ticket letting the team know it's been assigned.
