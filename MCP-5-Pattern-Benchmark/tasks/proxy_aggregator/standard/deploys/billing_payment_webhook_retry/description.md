The billing-service repo has an open change request titled "Add retry to
payment webhook". Confirm its current status before deploying.

Create a staging deploy for the billing-service repo, then mark that
deploy succeeded once it completes.
