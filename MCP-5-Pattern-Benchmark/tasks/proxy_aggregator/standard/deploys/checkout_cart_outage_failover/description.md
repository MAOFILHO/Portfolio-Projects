The checkout-web repo has a runbook titled "Handling a cart-service outage"
— read it for the failover steps before proceeding.

Create a production deploy for the checkout-web repo, then mark that deploy
succeeded once it completes.
