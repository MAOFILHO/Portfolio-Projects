The checkout-web repo has a runbook titled "Clearing the checkout cache" —
read it for the steps before proceeding.

Create a staging deploy for the checkout-web repo, then mark that deploy
succeeded once it completes.
