The notifications-service repo has a runbook titled "Draining the
notification queue" — read it for the steps before proceeding.

Create a staging deploy for the notifications-service repo, then mark that
deploy succeeded once it completes.
