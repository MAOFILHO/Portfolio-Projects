The notifications-service repo has an open change request titled "Fix
duplicate SMS alerts". Confirm its current status, then consult the runbook
titled "Recovering from an SMS provider outage" for guidance before
deploying notifications-service.

Create a production deploy for the notifications-service repo. The rollout
fails — mark that deploy failed.
