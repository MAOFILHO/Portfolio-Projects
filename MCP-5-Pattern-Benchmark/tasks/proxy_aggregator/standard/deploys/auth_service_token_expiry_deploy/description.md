The auth-service repo has an open change request titled "Fix auth token
expiry check". Confirm its current status before deploying.

Create a production deploy for the auth-service repo, then mark that deploy
succeeded once it completes.
