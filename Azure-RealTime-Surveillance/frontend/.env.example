# Local dev only. In production these are generated by
# src/surveil_deploy/steps/s08_env_and_config.py into .env.production.
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws/alerts
# Leave empty for local dev -- the backend's frame_upload_api_key check is a
# no-op when unset (see backend/app/deps.py).
VITE_API_KEY=
